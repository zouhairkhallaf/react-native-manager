/** @format */

import {AppRegistry} from 'react-native';
import App from './src/App';
//import {name as appName} from './app.json';  // Use this as the config file if you want to put the name of the application in it.

AppRegistry.registerComponent('manager', () => App);
